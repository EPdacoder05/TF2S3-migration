# Core dependencies (stdlib only - no external deps required)
# The tool uses Python standard library + CLI tools (aws, terraform, gh)

# Development dependencies
# Install with: pip install -r requirements.txt
ruff>=0.8.0
mypy>=1.13.0
pytest>=8.0.0
pytest-cov>=6.0.0
bandit[toml]>=1.8.0
